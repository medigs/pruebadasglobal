<p style="color: green"><%= notice %></p>
<div class="pagetitle">
  <h1>Sucursales</h1>
</div><!-- End Page Title -->
<hr>
<section class="section">
  <div class="row align-items-top">
    <div class="col-lg-12">

      <!-- Default Card -->
      <div class="card">
        <div class="card-body">
          <div class="row">
            <div class="col-lg-8">
              <h5 class="card-title">Listado de Sucursales</h5>
            </div>
            <div class="col-lg-4 float-right">
              <%= link_to "<i class='bi bi-bookmark-plus-fill'></i>Nuevo registro".html_safe, root_path, class: "text-success", data: { turbolinks: false }  %>
            </div>
          </div>
          <table class="table datatable" id="empresa_table" data-controller="datatable" data-turbolinks="false" class="paginacion">
            <thead>
              <tr>
                <th scope="col">Nombre</th>
                <th scope="col">Direccion</th>
                <th scope="col">Telefono</th>
                <th scope="col">Colaboradores</th>
              </tr>
            </thead>
            <tbody>
              <% @sucursales.each do |sucursal| %>
                <tr>
                  <td><%= sucursal.nombre %></td>
                  <td><%= sucursal.direccion %></td>
                  <td><%= sucursal.telefono %></td>
                  
                  <td>
                    <a href="/colaboradores/<%= sucursal.id %>" class="btn btn-primary btn-circle btn-sm" data-toggle="tooltip" data-placement="top" title="Mostrar">
                        <i class="bi bi-eye-fill text-white"></i> Ver
                    </a>
                  </td>

                </tr>
              <% end %>
            </tbody>
          </table>
            <a href="/empresas" class="btn btn-info " data-toggle="tooltip" data-placement="top" title="Regreasar">
               <i class="bi bi-skip-backward"></i> Regresar a empresas
            </a>
        </div>
      </div><!-- End Default Card -->
    </div>
  </div>
</section>